import logoUrl from '@/assets/icon-32.png';

export const NIMUE_LOGO_URL = logoUrl;

/** Donation / support page — set this when ready; opens in a new browser tab. */
export const NIMUE_SUPPORT_URL = '';
